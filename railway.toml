[build]
builder = "nixpacks"
buildCommand = "bundle install && bun install && bun run build && bundle exec rails db:migrate"

[deploy]
startCommand        = "bundle exec puma -C config/puma.rb"
healthcheckPath     = "/up"
healthcheckTimeout  = 60
restartPolicyType   = "on_failure"
restartPolicyMaxRetries = 3
